import getRFSAPI from '../src';

describe('@geeeger/get-rfs-api module', () => {
  it('should pass', () => {
    // 使用jest-jsdom环境无法覆盖完全
    // expect(getRFSAPI()).toBe('requestFullscreen');
  });
});
